# Secret template for the backend connection to MongoDB Atlas.
apiVersion: v1
kind: Secret
metadata:
  # Referenced by backend deployment.
  name: mongodb-secret
  namespace: todo-app
# Opaque is the generic Kubernetes key/value secret type.
type: Opaque
# stringData lets you provide plain strings; Kubernetes stores them as base64-encoded data.
stringData:
  # Backend connection string for MongoDB Atlas.
  MONGODB_URI: "mongodb+srv://mekin:test@cluster0.ndaq9wx.mongodb.net/?appName=Cluster0"
